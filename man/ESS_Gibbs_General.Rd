% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TO_General.R
\name{ESS_Gibbs_General}
\alias{ESS_Gibbs_General}
\title{ESS-within-Gibbs sampler for target-only Bayesian high-dimensional uncensored regression}
\usage{
ESS_Gibbs_General(
  X,
  Y,
  b.c = NULL,
  sd.0 = NULL,
  lambda = NULL,
  xi = NULL,
  sd_y = NULL,
  b0 = NULL,
  intercept = TRUE,
  N = 5000,
  S.max = 100,
  block_size = 1,
  family = "Gaussian",
  df = 4,
  slab = "exp",
  verbose = 1,
  debug = F
)
}
\arguments{
\item{X}{Design matrix.}

\item{Y}{Response.}

\item{b.c}{Initial states for latent parameters.}

\item{sd.0}{Prior standard deviations of the latent parameters.}

\item{lambda}{Threshold parameter; defaults to the square root of the number of predictors, sqrt(p).}

\item{xi}{shadow variable for slab scale parameter.}

\item{sd_y}{Initial outcome standard deviation for 'Gaussian' or scale for 'Student-t'. For 'Negative-binomial' and 'Gamma', this is the positive shape; for 'Beta', the positive precision. Defaults to 1. Fixed at 1 and unused for 'Logistic' and 'Poisson'.}

\item{b0}{Initial value for the intercept term, default is 0.}

\item{intercept}{Whether to estimate the intercept term in the MCMC algorithm, default is TRUE.}

\item{N}{Number of MCMC iterations.}

\item{S.max}{Maximum slice iterations per update.}

\item{block_size}{Block size for updates.}

\item{family}{Outcome distribution: 'Gaussian', 'Logistic' (binary 0/1), 'Student-t', 'Poisson' or 'Negative-binomial' (nonnegative integer counts, log link), 'Gamma' (positive response, log link), or 'Beta' (response strictly between 0 and 1, logit link). Case-insensitive; default is 'Gaussian'.}

\item{df}{Fixed positive degrees of freedom for 'Student-t', default is 4. Ignored for other families.}

\item{slab}{Slab transformation: 'exp', 'poly', 'nlp', or 'guassian'.}

\item{verbose}{Verbosity flag.}

\item{debug}{Optional returning of MCMC runs other than the coefficient itself.}
}
\value{
A list containing MCMC draws and diagnostics. mc_sigma stores the outcome scale, shape, or precision according to family.
}
\description{
Runs elliptical slice sampling updates for target parameters.
}
\details{
Shape/precision is updated by log-scale Metropolis-Hastings with proposal standard deviation 0.3 for these three families. The negative-binomial shape has an inverse-gamma prior with shape 0.4 and scale 0.3; gamma shape and beta precision have Gamma(shape = 0.01, rate = 0.01) priors.
}
