% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TL_General.R
\name{ESS_Gibbs_TL_General}
\alias{ESS_Gibbs_TL_General}
\title{ESS-within-Gibbs sampler for Bayesian transfer learning of uncensored regression models}
\usage{
ESS_Gibbs_TL_General(
  X_T,
  Y_T,
  X_s,
  Y_s,
  bt.c = NULL,
  bs.c = NULL,
  lambda_T = NULL,
  lambda_s = NULL,
  xi = NULL,
  xi_s = NULL,
  xi_prior = 1,
  sig_T = NULL,
  sig_s = NULL,
  b0_T = NULL,
  b0_s = NULL,
  intercept = TRUE,
  N = 5000,
  S.max = 500,
  block_size = 1,
  family = "Gaussian",
  df = 4,
  slab = "exp",
  verbose = 1,
  debug = F
)
}
\arguments{
\item{X_T}{Target design matrix.}

\item{Y_T}{Target response.}

\item{X_s}{List of source design matrices.}

\item{Y_s}{List of source responses.}

\item{bt.c, bs.c}{Initial states for target and source parameters.}

\item{lambda_T, lambda_s}{Threshold parameters. The target defaults to sqrt(p), where p is the number of predictors; each source defaults to 3.}

\item{xi, xi_s}{Initial states of shadow variables for the slab scale parameter.}

\item{xi_prior}{Prior standard deviation for the target slab-scale shadow variable.}

\item{sig_T, sig_s}{Initial target/source outcome standard deviations for 'Gaussian' or scales for 'Student-t'. For 'Negative-binomial' and 'Gamma', this is the positive shape; for 'Beta', the positive precision. Defaults to 1. Fixed at 1 and unused for 'Logistic' and 'Poisson'.}

\item{b0_T, b0_s}{Initial value for the intercept term, default is 0.}

\item{intercept}{Whether to estimate the intercept term in the MCMC algorithm, default is TRUE.}

\item{N}{Number of MCMC iterations.}

\item{S.max}{Maximum slice iterations per update.}

\item{block_size}{Block size for updates.}

\item{family}{Outcome distribution: 'Gaussian', 'Logistic' (binary 0/1), 'Student-t', 'Poisson' or 'Negative-binomial' (nonnegative integer counts, log link), 'Gamma' (positive response, log link), or 'Beta' (response strictly between 0 and 1, logit link). Case-insensitive; default is 'Gaussian'.}

\item{df}{Fixed positive degrees of freedom for 'Student-t', default is 4. Ignored for other families.}

\item{slab}{Slab transformation: 'exp', 'poly', 'nlp1', 'nlp2', or 'guassian'.}

\item{verbose}{Verbosity flag.}

\item{debug}{Optional returning of MCMC runs other than the coefficient itself.}
}
\value{
A list containing MCMC draws and diagnostics. mc_sig_T and mc_sig_s store target and source outcome scales, shapes, or precisions according to family.
}
\description{
Runs elliptical slice sampling updates for target parameters and source biases.
}
\details{
Each study has a separate shape/precision parameter with an independent prior, updated by log-scale Metropolis-Hastings with proposal standard deviation 0.3 for these three families. The negative-binomial shape has an inverse-gamma prior with shape 0.4 and scale 0.3; gamma shape and beta precision have Gamma(shape = 0.01, rate = 0.01) priors.
}
